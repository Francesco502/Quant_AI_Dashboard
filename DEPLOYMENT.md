# Deployment

The current deployment guide lives at [`docs/current/deployment.md`](./docs/current/deployment.md).

The canonical container deployment entrypoint is [`docker-compose.yml`](./docker-compose.yml).
