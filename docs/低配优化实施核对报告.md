# 低配服务器优化实施核对报告

> 核对日期：2026-02-24 | 依据文档：[低配服务器优化实施方案.md](低配服务器优化实施方案.md)

---

## 一、版本号 3.0.0 核对结果 ✅

| 位置 | 当前值 | 状态 |
|------|--------|------|
| `core/version.py` | `__version__ = "3.0.0"`，major=3 | ✅ |
| `web/package.json` | `"version": "3.0.0"` | ✅ |
| `docker-compose.yml`（backend） | `image: francescoli/quant_backend:v3.0.0` | ✅ |
| `docker-compose.yml`（frontend） | `image: francescoli/quant_frontend:v3.0.0` | ✅ |
| `docker-compose.yml`（daemon） | `image: francescoli/quant_backend:v3.0.0` | ✅ |
| `docs/低配服务器优化实施方案.md` | 适用项目版本 v3.0.0 | ✅ |
| `DEPLOY_OPTIMIZED.md` | 适用系统 v3.0.0+ | ✅ |
| API 根路径/健康检查 | 使用 `core.version.VERSION`（3.0.0） | ✅ |

说明：`web/package-lock.json` 中的 `"2.0.0"` 等为**第三方依赖**版本（如 tslib、convert-source-map），非本项目版本，无需修改。

---

## 二、优化实施方案完成度

### 第一阶段 — 立竿见影 ✅

| 序号 | 任务 | 修改文件 | 状态 |
|------|------|---------|------|
| 1.1 | 前端改为静态导出 | `web/next.config.ts`（output: "export", images.unoptimized） | ✅ |
| 1.2 | SQLite 内存参数调降 | `core/database_pool.py`（cache_size/mmap_size/temp_store） | ✅ |
| 1.3 | Uvicorn 降为单 Worker | `docker-compose.yml`（workers 1, limit-concurrency 8） | ✅ |

### 第二阶段 — 核心优化 ✅

| 序号 | 任务 | 修改文件 | 状态 |
|------|------|---------|------|
| 2.1 | 禁用 LSTM/PyTorch | `requirements.txt`（torch 注释）、`core/advanced_forecasting.py`（_normalize_model_type、DISABLE_HEAVY_MODELS） | ✅ |
| 2.2 | 模型管理器参数调降 | `core/ml_model_manager.py`（max_memory_mb 200、auto_unload_after 120、interval 30） | ✅ |
| 2.3 | 缓存层缩容 | `core/multi_level_cache.py`（l1_max_size 200）、`core/lru_cache.py`（max_size 50、max_memory_mb 20） | ✅ |
| 2.4 | 数据获取并发降级 | `core/data_service.py`（8→3、4→2）、`core/market_scanner.py`（4→2） | ✅ |

### 第三阶段 — 稳定性加固 ✅

| 序号 | 任务 | 修改文件 | 状态 |
|------|------|---------|------|
| 3.1 | 训练任务错峰到凌晨 | `core/daemon.py` 默认配置 `training.time: "02:00"` | ✅ |
| 3.2 | 数据更新频率 1h→2h | `core/daemon.py` 默认配置 `data_update.interval_minutes: 120` | ✅ |
| 3.3 | 添加训练前内存检查 | `core/training_pipeline.py`（可用内存 <400MB 跳过） | ✅ |
| 3.4 | 添加内存熔断机制 | `core/daemon.py`（_memory_guard，三处任务入口调用） | ✅ |
| 3.5 | 训练完成后强制 GC | `core/training_pipeline.py`（run_training_job 每标的后 gc.collect()） | ✅ |

### 第四阶段 — 部署层优化 ✅

| 序号 | 任务 | 说明 | 状态 |
|------|------|------|------|
| 4.1 | 单容器 Dockerfile | `Dockerfile.optimized` 已存在 | ✅ |
| 4.2 | Supervisor 配置 | `docker/supervisord.conf` 已存在 | ✅ |
| 4.3 | 优化版 Nginx 配置 | `docker/nginx.conf` 已存在 | ✅ |
| 4.4 | 优化版 docker-compose | `docker-compose.optimized.yml` 已存在（含资源限制） | ✅ |
| 4.5 | 内存/CPU 限制 | `docker-compose.optimized.yml` 中 memory: 1.5G, cpus: 1.5 | ✅ |

### 文档建议项（已落实）

| 项 | 说明 | 状态 |
|----|------|------|
| 健康检查暴露内存 | 方案七 7.1 建议 `/api/health` 返回内存信息 | ✅ 已加入 `api/main.py` |

---

## 三、配置参数速查表核对

与方案文档「六、配置参数速查表」一致：

| 文件 | 参数 | 文档要求 | 当前实现 |
|------|------|----------|----------|
| web/next.config.ts | output | "export" | ✅ "export" |
| core/database_pool.py | cache_size | -4096 (4MB) | ✅ -4096 |
| core/database_pool.py | mmap_size | 33554432 (32MB) | ✅ 33554432 |
| core/database_pool.py | temp_store | FILE | ✅ FILE |
| core/ml_model_manager.py | max_memory_mb | 200.0 | ✅ 200.0 |
| core/ml_model_manager.py | auto_unload_after | 120.0 | ✅ 120.0 |
| core/ml_model_manager.py | auto_unload_interval | 30.0 | ✅ 30.0 |
| core/multi_level_cache.py | l1_max_size | 200 | ✅ 200 |
| core/lru_cache.py | max_size | 50 | ✅ 50 |
| core/lru_cache.py | max_memory_mb | 20.0 | ✅ 20.0 |
| core/data_service.py | 批量 max_workers | 3 | ✅ 3 |
| core/data_service.py | 其他 max_workers | 2 | ✅ 2 |
| core/market_scanner.py | max_workers | 2 | ✅ 2 |
| docker-compose.yml | workers | 1 | ✅ 1 |
| docker-compose.yml | limit-concurrency | 8 | ✅ 8 |
| daemon 默认配置 | training.time | "02:00" | ✅（daemon.py 默认） |
| daemon 默认配置 | data_update.interval_minutes | 120 | ✅（daemon.py 默认） |
| requirements.txt | torch | 注释掉 | ✅ 已注释 |

---

## 四、结论

- **版本号**：所有**项目版本**已统一为 **3.0.0**（仅第三方依赖版本未改，且无需改）。
- **优化方案**：方案文档中**第一至第四阶段**及「配置参数速查表」均已完成；文档建议的「健康检查返回内存」已实现。

建议按文档「八、验证清单」在目标 2 核 2GB 环境做一次部署与功能验证。
